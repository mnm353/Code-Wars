//function checkAge(age) {
  //  var oldEnough = age >= 21 ? 'Yup' : 'Nope' ;
    //console.log(oldEnough) ;
  //}

  function addTen( arr ) {
    var newArr = [];
    
    for( var i = 0; i < arr.length; i++ ) {
      if(type === "string") {
         newArr.push = Number(arr);
      }
    return newArr[i] + 10;
    }
  }